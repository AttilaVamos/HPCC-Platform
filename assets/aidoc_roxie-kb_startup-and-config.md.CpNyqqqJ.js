import{_ as i,o,a,ag as t}from"./chunks/framework.Do1Zayaf.js";const h=JSON.parse('{"title":"Roxie Startup and Query Loading Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"aidoc/roxie-kb/startup-and-config.md","filePath":"aidoc/roxie-kb/startup-and-config.md","lastUpdated":1779520499000}'),n={name:"aidoc/roxie-kb/startup-and-config.md"};function r(s,e,l,c,d,u){return o(),a("div",null,e[0]||(e[0]=[t('<h1 id="roxie-startup-and-query-loading-architecture" tabindex="-1">Roxie Startup and Query Loading Architecture <a class="header-anchor" href="#roxie-startup-and-query-loading-architecture" aria-label="Permalink to &quot;Roxie Startup and Query Loading Architecture&quot;">​</a></h1><p>This document describes the initialization sequence and scaling bottlenecks for the Roxie component, specifically analyzing <code>roxie/ccd/ccdmain.cpp</code> and <code>roxie/ccd/ccdstate.cpp</code>.</p><h2 id="entry-sequence-roxie-main" tabindex="-1">Entry Sequence (<code>roxie_main</code>) <a class="header-anchor" href="#entry-sequence-roxie-main" aria-label="Permalink to &quot;Entry Sequence (`roxie_main`)&quot;">​</a></h2><p>The execution begins in <code>roxie_main</code>, executing several critical initializations that dictate engine topology, networking capabilities, and memory boundaries.</p><ol><li><strong>Topology &amp; Configuration Binding</strong>: Settings are aggregated from <code>loadConfiguration</code> (falling back to YAML or XML topology). It dynamically injects workunit-level configurations if executed directly upon a WorkUnit, allowing testing settings to override persistent configs.</li><li><strong>Mode Switch (One-Shot vs. Daemon)</strong>: <ul><li>If loading a standalone query DLL or specific WorkUnit (<code>--selftest</code> or WUID argument), <code>oneShotRoxie</code> / <code>runOnce</code> modes apply.</li><li>Single-use mode skips heavy file-cache workers, omits large agent loop setups, and bypasses plugin preloads (<code>queryFileCache().start()</code> and <code>loadPlugins()</code> are bypassed).</li></ul></li><li><strong>Hardware / Resource Probing</strong>: Allocates logical constraints (<code>totalMemoryLimit</code>, huge-pages rules, <code>numAgentThreads</code>, <code>numServerThreads</code>). Computes concurrency settings and bounds I/O chunking (<code>indexReadChunkSize</code>, <code>mtu_size</code> for UDP/TCP max limits).</li><li><strong>Memory Structures</strong>: Sets limits for custom memory managers (<code>roxiemem::setTotalMemoryLimit</code>), binds <code>nodeCacheMem</code> and <code>leafCacheMem</code> tightly to <code>jhtree</code> bounds which inherently affect scaling.</li><li><strong>Networking Pre-flight</strong>: Resolves multicast listeners (<code>ccdMulticastPort</code>). Determines worker network topology structure based on cluster layout.</li></ol><h2 id="query-loading-and-concurrency" tabindex="-1">Query Loading and Concurrency <a class="header-anchor" href="#query-loading-and-concurrency" aria-label="Permalink to &quot;Query Loading and Concurrency&quot;">​</a></h2><p>The ultimate bottleneck dictates how fast a Roxie pod or node can service traffic: <strong>Loading the Query Set and Binding File Indexes</strong>.</p><p>When <code>globalPackageSetManager-&gt;load()</code> executes, it dives into <code>ccdstate.cpp</code>, which manages query set deployment. The speed at which queries bind relies on two fundamental concurrency levels:</p><ol><li><code>parallelQueryLoadThreads</code> (Default: 1, scaled in config)</li><li><code>numResolveFilenameThreads</code> (Default: <code>parallelQueryLoadThreads * 4</code> if using remote storage)</li></ol><h3 id="execution-flow-of-query-loading" tabindex="-1">Execution Flow of Query Loading <a class="header-anchor" href="#execution-flow-of-query-loading" aria-label="Permalink to &quot;Execution Flow of Query Loading&quot;">​</a></h3><p>To prevent blocking initialization indefinitely against cloud storage architectures (e.g. Blob/S3 or remote NFS queries):</p><ol><li><strong>Phase 1: WorkUnit Expansion (<code>parallelQueryLoadThreads</code>)</strong><ul><li>Ingests the query XML via <code>createQueryDll</code>.</li><li>Iterates the WorkUnit components. Instead of resolving file locations inline, it gathers all <code>SummaryType::ReadIndex</code> and <code>SummaryType::ReadFile</code> dependencies and generates logical paths (<code>expandLogicalFilename</code>).</li><li>Paths are tracked in a unified package <code>SummaryMap</code> inside a lightweight lock <code>CriticalSection filenameCrit</code>.</li></ul></li><li><strong>Phase 2: I/O File Resolution (<code>numResolveFilenameThreads</code>)</strong><ul><li>Iterates physical names generated in Phase 1 asynchronously.</li><li>For remote environments, retrieving file sizes from remote storage happens heavily in parallel.</li><li>Files are cached via <code>package-&gt;lookupExpandedFileName(...)</code> so subsequent lookups are O(1).</li></ul></li><li><strong>Phase 3: Index Pre-Opening (The Chokepoint)</strong><ul><li>If <code>preopenActiveIndexes</code> is true, eagerly checks <code>resolvedfile-&gt;getKeyArray()</code> and loops all active file parts to call <code>thisKey-&gt;ensureReady()</code>. This pulls node roots into memory. Pre-opening prevents the <em>first query penalty</em> but slows down container scale-outs.</li></ul></li><li><strong>Phase 4: Factory Instantiation (<code>parallelQueryLoadThreads</code>)</strong><ul><li>Assembles the validated dll resources calling <code>loadQueryFromDll</code>, binding execution state graphs, and finalizing query insertion to Roxie&#39;s active map.</li></ul></li></ol><h2 id="scaling-time-optimization-details" tabindex="-1">Scaling Time Optimization Details <a class="header-anchor" href="#scaling-time-optimization-details" aria-label="Permalink to &quot;Scaling Time Optimization Details&quot;">​</a></h2><p>Several techniques and toggles govern Roxie&#39;s startup latency constraints:</p><ul><li><strong>Lazy Opening (<code>@lazyOpen</code>)</strong>: The default is <code>smart</code>. If <code>restarts &gt; 0</code> (meaning a crash or container restart), <code>lazyOpen</code> switches to <code>true</code>. This skips index validation entirely at startup to bring the server immediately online, passing the latency inherently down to the subsequent first query that tries hitting the missing/unverified storage nodes.</li><li><strong>Dependency Execution</strong>: Options such as <code>defaultExecuteDependenciesSequentially</code> dictate if loaded sub-queries delay initialization.</li><li><strong>File Timestamp / Hash Validation</strong>: Verifying that <code>alwaysTrustFormatCrcs</code>, <code>ignoreFileDateMismatches</code>, and <code>ignoreFileSizeMismatches</code> are safely bypassed inside the resolution loop minimizes Dali calls or FileSystem metadata probing.</li><li><strong>Node Hash Caching</strong>: During file verification, the query set computes a hash string of all dependencies. If the hashes match, DLLs and factory instances can bypass parsing WorkUnit components (<code>addQuery</code>).</li></ul><h2 id="key-learnings-for-architectural-enhancements" tabindex="-1">Key Learnings for Architectural Enhancements <a class="header-anchor" href="#key-learnings-for-architectural-enhancements" aria-label="Permalink to &quot;Key Learnings for Architectural Enhancements&quot;">​</a></h2><ul><li>Modifying <code>preopenActiveIndexes</code> in containerized environments allows orchestrators to register readiness faster.</li><li>Modifying <code>parallelQueryLoadThreads</code> is essential on cores with high count but must be weighed against CPU starvation.</li><li>Overhauling <code>roxie_main</code>&#39;s initialization sequencing requires acknowledging single-mode (<code>oneShotRoxie</code>) vs farm processes; memory must initialize before topologies load DLLs, and caching must initiate only after DLL factory binding is determined parallel-safe.</li></ul>',17)]))}const p=i(n,[["render",r]]);export{h as __pageData,p as default};
