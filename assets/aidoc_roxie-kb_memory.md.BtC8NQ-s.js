import{_ as a,o,a as i,ag as t}from"./chunks/framework.Do1Zayaf.js";const p=JSON.parse('{"title":"Roxie Memory Architecture & Analysis","description":"","frontmatter":{},"headers":[],"relativePath":"aidoc/roxie-kb/memory.md","filePath":"aidoc/roxie-kb/memory.md","lastUpdated":1779520499000}'),n={name:"aidoc/roxie-kb/memory.md"};function r(l,e,s,c,d,h){return o(),i("div",null,e[0]||(e[0]=[t('<h1 id="roxie-memory-architecture-analysis" tabindex="-1">Roxie Memory Architecture &amp; Analysis <a class="header-anchor" href="#roxie-memory-architecture-analysis" aria-label="Permalink to &quot;Roxie Memory Architecture &amp; Analysis&quot;">​</a></h1><p><strong>Overview</strong>: The roxiemem component provides custom, highly-performant heap allocation schemes (CChunkingRowManager) designed around query lifetime limits and block allocators optimized for Roxie&#39;s execution model.</p><h2 id="allocation-hierarchy-and-structures" tabindex="-1">Allocation Hierarchy and Structures <a class="header-anchor" href="#allocation-hierarchy-and-structures" aria-label="Permalink to &quot;Allocation Hierarchy and Structures&quot;">​</a></h2><ol><li><strong>Row Manager (CChunkingRowManager in roxie/roxiemem/roxiemem.cpp)</strong>: <ul><li>Manages an array of size-classified heaps (normalHeaps) of type CFixedChunkedHeap.</li><li>Also dynamically manages per-activity blocks, and huge allocations (CHugeHeap). All are governed by severe memory footprint tracking (callbacks, OOM handling).</li></ul></li><li><strong>Chunked Heaps (CChunkedHeap and derivatives)</strong>: <ul><li>Contains a pool of ChunkedHeaplet (or FixedSizeHeaplet/PackedFixedSizeHeaplet) instances.</li><li>Handles the fixed-size row layout allocating chunks aligned to HEAP_ALIGNMENT_SIZE boundaries.</li><li>CHeap::heapletLock (a CriticalSection) gates the allocation process.</li></ul></li><li><strong>Heaplet (ChunkedHeaplet)</strong>: <ul><li>Represents an exact 256kb block (by default) HEAP_ALIGNMENT_SIZE which serves equally-sized requests.</li><li>Frees rows via inlineReleasePointer using a Lock-Free logic.</li></ul></li></ol><h2 id="concurrency-and-deallocation-the-fast-path-aba-solution" tabindex="-1">Concurrency and Deallocation (The Fast-Path &amp; ABA Solution) <a class="header-anchor" href="#concurrency-and-deallocation-the-fast-path-aba-solution" aria-label="Permalink to &quot;Concurrency and Deallocation (The Fast-Path &amp; ABA Solution)&quot;">​</a></h2><ul><li><strong>Lock-Free Deallocations</strong>: <ul><li>While allocations are guarded by heapletLock (an OS CriticalSection / Spinlock), freeing memory via FixedSizeHeaplet::noteReleased (which calls inlineReleasePointer) operates entirely lock-free using std::atomic operations.</li><li>This severely limits contention when thousands of threads tear down datasets.</li></ul></li><li><strong>ABA Problem Prevention</strong>: <ul><li>Adding rows back to the chunk free-list (r_blocks linked structure) via compare_exchange_efficient(r_blocks...) in inlineReleasePointer suffers inherently from the ABA problem.</li><li>roxiemem defends against this by masking the top portion of the 32-bit r_blocks value as an incrementing CAS tag (RBLOCKS_CAS_TAG_MASK).</li></ul></li></ul><h2 id="noteworthy-options" tabindex="-1">Noteworthy Options <a class="header-anchor" href="#noteworthy-options" aria-label="Permalink to &quot;Noteworthy Options&quot;">​</a></h2><ul><li><strong>Scanning Allocator (ALWAYS_USE_SCAN_HEAP)</strong>: Currently commented out, but there exists a lock-free linear scan allocator alternative. It attempts to scan tags linearly instead of using the r_blocks free list, but suffers pathological costs when fragmented memory forces massive numbers of cache-miss scans (&quot;Excessive scans&quot;).</li></ul><h2 id="bottlenecks-contention-points" tabindex="-1">Bottlenecks &amp; Contention Points <a class="header-anchor" href="#bottlenecks-contention-points" aria-label="Permalink to &quot;Bottlenecks &amp; Contention Points&quot;">​</a></h2><ol><li><strong>Allocation Serialization (heapletLock)</strong>: Since CChunkedHeap::doAllocateRow takes a lock before checking its activeHeaplet list, highly concurrent identical-size queries strictly serializing on heapletLock.</li><li><strong>Buffer States (activeBufferCS)</strong>: CChunkingRowManager::activeBuffs has a critical section activeBufferCS explicitly marked // Potentially slow.</li><li><strong>Dynamic Heaps (fixedSpinLock)</strong>: CChunkingRowManager::fixedSpinLock synchronizes fixedHeaps list access and reportEmptyHeaps. Marked &quot;possibly be a ReadWriteLock&quot;.</li></ol><h2 id="standard-heap-vs-roxiemem-jemalloc-fallback-analysis" tabindex="-1">Standard Heap vs. Roxiemem (Jemalloc Fallback analysis) <a class="header-anchor" href="#standard-heap-vs-roxiemem-jemalloc-fallback-analysis" aria-label="Permalink to &quot;Standard Heap vs. Roxiemem (Jemalloc Fallback analysis)&quot;">​</a></h2><p>Many peripheral objects in Roxie execution paths rely on the standard C++ heap (backed by <code>jemalloc</code>). While <code>jemalloc</code> is generally performant, short-lived, frequently resizing objects bypass <code>roxiemem</code> and incur overhead:</p><ul><li><strong>StringBuffer/SCMStringBuffer</strong>: Heavily utilized for string manipulation, query parameter packing/unpacking, and generating result labels (e.g., in <code>roxie/ccd/ccdquery.cpp</code> and <code>roxie/ccd/ccdserver.cpp</code>). Successive appends cause <code>realloc</code> operations, triggering tcache thrashing, fragmentation, and potential bin-lock contention for medium-sized buffers.</li><li><strong>MemoryBuffer</strong>: Used extensively for network data serialization (e.g., <code>MemoryBuffer</code> metadata inside <code>roxie/udplib/udpmsgpk.cpp</code> and packet collating). As data chunks are read from indexes across nodes, arrays re-size aggressively.</li><li><strong>IPropertyTree (<code>jptree.hpp</code>)</strong>: Standard properties tree components used for XML/JSON payload processing parse inputs into numerous intermediate heap-allocated strings, lists, and property nodes, none of which utilize <code>roxiemem</code>&#39;s chunked fast-release.</li></ul><p><strong>Inefficiencies Explained</strong>:</p><ul><li><strong>Tcache Thrashing</strong>: Creating thousands of temporary strings and intermediate parsing nodes pollutes <code>jemalloc</code>&#39;s thread-local cache. When the tcache overflows, memory gets evicted to the central arena requiring locks.</li><li><strong>Lock Contention</strong>: While <code>jemalloc</code> employs arenas to limit contention, high concurrency across all threads competing for string allocations simultaneously on hot boundary paths natively induces lock stalls compared to <code>roxiemem</code>&#39;s tailored lock-free deallocations (<code>inlineReleasePointer</code>).</li><li><strong>TLB Misses &amp; Fragmentation</strong>: Rapid recycling of unpredictable array sizes (growing via <code>MemoryBuffer</code> limits) leads to heavy virtual memory un-mappings/re-mappings. TLB entries get invalidated, resulting in higher latency over multi-gigabyte heap traversals per query.</li></ul>',15)]))}const m=a(n,[["render",r]]);export{p as __pageData,m as default};
